# hamboo 工作台云部署 · 项目梳理

> 目标：将 WorkBuddy 生成的单文件 HTML 工作台改造为 iPad/iPhone/Windows 三端云同步版本  
> 更新时间：2026-07-30

---

## 一、做了什么

### 1. 代码改造

| 步骤 | 内容 |
|------|------|
| 原始文件分析 | 读取 `index.html`（2500+ 行单文件工作台，含待办、账号矩阵、选题、饮食、账本、体重、照片等模块） |
| 方案设计 | 确定 **Supabase（后端） + PWA（离线可用） + 静态托管（部署）** 架构 |
| 代码生成 | 创建了 6 个文件：`auth.js`、`db.js`、`supabase.js`、`manifest.json`、`sw.js`、`schema.sql` |
| HTML 改造 | Python 脚本批量替换：`localStorage` → `cloudSave/cloudLoad`、添加登录界面、移除备份卡片、更新提示文案 |
| auth.js 修复 | 首版注册后界面卡"处理中"，v2 修复为注册成功自动切换登录模式 + 中文错误提示 |

### 2. Supabase 后端

| 步骤 | 内容 |
|------|------|
| 注册/登录 | Playwright 浏览器操作，通过 GitHub 登录 Wade-YD 账号 |
| 创建项目 | `hamboo-workspace`，新加坡节点（ap-southeast-1），免费计划 |
| 建表 | `profiles`（用户档案 + 自动触发器）+ `workspace_data`（统一数据表） |
| 安全配置 | RLS 策略：`own_profile` + `own_data`，每人只能读写自己的数据 |
| 密钥写入 | `supabase.js` 已填入项目 URL 和 publishable key |

### 3. 部署尝试（按时间线）

| 序号 | 平台 | 方式 | 结果 |
|------|------|------|------|
| ① | **Vercel** | GitHub 登录 | ❌ 账号被拦截，需额外验证 |
| ② | **Cloudflare Pages** | Wrangler CLI `login` | ❌ 非交互环境 OAuth 回调失败 |
| ③ | **GitHub Pages** | 创建仓库 + `git push` | ✅ 部署成功 `wade-yd.github.io/hamboo-workspace` |
| ④ | **Cloudflare Pages** | Wrangler + API Token | ❌ Token 权限不足（返回 0 个账户） |

> **结论**：GitHub Pages 已经可用，Cloudflare 可后续补充。

---

## 二、遇到的问题

| 问题 | 根因 | 状态 |
|------|------|------|
| **注册卡"处理中"** | `auth.js` 注册成功后没正确处理 session 缺失（Supabase 可能要求邮件确认） | ✅ 代码已修复，已推送 |
| **登录报"网络错误"** | Supabase publishable key 可能被截断（`sb_publishable_O1QZ5...` 较短，Supabase 后台 UI 折叠导致） | ❌ 未验证 |
| **Vercel 登录拦截** | 账号 `user_blocked`，需额外验证 | ❌ 已放弃此方案 |
| **Wrangler login 失败** | PowerShell 非交互环境不支持 OAuth 回调节流 | ❌ 已改用 API Token |
| **Cloudflare API Token 权限不足** | 创建时没勾 `Account Settings: Read` | ❌ 需重建 Token |
| **Supabase/Cloudflare 后台渲染异常** | 两个平台后台在 Playwright 里 JS 渲染不完整，反复遇到 | ⚠️ 环境限制 |

---

## 三、当前项目状态

| 项目 | 状态 | 详情 |
|------|------|------|
| 代码 | ✅ 完成 | `D:\桌面\私人图书馆\7-工具箱\工作台\cloud\`（7 个文件） |
| GitHub 仓库 | ✅ 已创建 | `Wade-YD/hamboo-workspace`，代码已推送 |
| GitHub Pages | ✅ 已部署 | `https://wade-yd.github.io/hamboo-workspace/` |
| Supabase 数据库 | ✅ 已建表 | profiles + workspace_data，含 RLS + Realtime |
| Supabase 认证 | ⚠️ 待确认 | 邮件确认状态未知（可能是注册卡住的根因） |
| 注册功能 | ❌ 卡住 | 界面停在"处理中" |
| 登录功能 | ❌ 网络错误 | 疑似 API key 不完整 |

---

## 四、待解决卡点（按优先级）

| 优先级 | 问题 | 下一步 |
|--------|------|--------|
| 🔴 P0 | 登录报"网络错误" | 到 Supabase 后台重新获取完整 API key，更新 `supabase.js` |
| 🔴 P0 | 注册卡"处理中" | 关闭 Supabase 邮件确认（Auth → Settings → 取消勾选 Confirm email） |
| 🟡 P1 | Cloudflare 部署 | 重建 API Token（需 `Account Settings: Read` + `Cloudflare Pages: Edit`）或继续用 GitHub Pages |
| 🟢 P2 | 实时同步验证 | P0 解决后，iPad/iPhone/PC 三端测试数据同步 |

---

## 五、文件清单

```
D:\桌面\私人图书馆\7-工具箱\工作台\cloud\
├── index.html      # 主页面（已改造，含登录 + Supabase 数据层）
├── supabase.js      # Supabase 配置（URL + Key）
├── auth.js          # 登录/注册逻辑（v2 修复版）
├── db.js            # 数据库读写 + 实时同步
├── schema.sql       # Supabase 建表 SQL（参考用）
├── manifest.json    # PWA 配置（iPad/iPhone 安装到桌面）
└── sw.js            # Service Worker（离线缓存）
```
